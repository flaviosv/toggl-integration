{
  "name": "toggl-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only MCP server for listing Toggl time entries by date range.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test 'dist/**/*.test.js'",
    "generate:openapi": "swagger2openapi openapi/toggl.swagger2.json --patch -o openapi/toggl.openapi3.json && openapi-typescript openapi/toggl.openapi3.json -o src/toggl/generated.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "dotenv": "^17.4.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "openapi-typescript": "^7.13.0",
    "swagger2openapi": "^7.0.8",
    "typescript": "^5.9.3"
  }
}
