# Required - server exits before registering any tool if any of these are missing or invalid.
TOGGL_API_TOKEN=changeme
TOGGL_WORKSPACE_ID=1234567

# Optional - documented defaults apply when unset.
# TOGGL_CACHE_PATH=~/.cache/toggl-mcp/projects.json
