# Fix Review Plan — PR #2 (TOGGL-2 Time Entries MCP)

All 65 threads classified **auto-fix** (no user comment beyond the posted finding itself, author `flaviosv` — the identity `gh` posts as). Source: complete-review's pending review, submitted as COMMENT.

## Cluster: mcp/src/tools/schemas.ts, mcp/src/tools/schemas.test.ts (new)

- **A3** (Low, Architecture) `mcp/src/tools/schemas.ts:23` — thread `PRRT_kwDOUA492s6chMbC`
  **[Architecture — A3, Low]** Per design.md, schemas.ts's stated purpose is 'shared validators.' `ToolDeps` (lines 23-27) is an unrelated concern — the DI contract for tool registration — bolted onto the same file. This is a minor SRP smell: one file now serves two purposes (request-shape validation and the collaborator-injection type) that would naturally evolve independently.
- **Q6** (Medium, Code Quality) `mcp/src/tools/schemas.ts:6` — thread `PRRT_kwDOUA492s6chMfW`
  **[Code Quality — Q6, Medium]** DATE_ONLY_PATTERN only checks digit shape (`\d{4}-\d{2}-\d{2}`) — unlike isValidRfc3339, it never calls Date.parse to reject an invalid calendar date (e.g. `2026-13-40`). Such a value passes dateOrTimestamp's refine. It's only caught later in list-time-entries.ts's cross-field `.refine` (toEpochMillis producing NaN), but that refine's message is hardcoded to "end_date must not be before start_date" attached to the `end_date` path — even when the actual problem is an invalid `start_date`, or both fields.
- **G7** (Low, Coverage Gaps) `mcp/src/tools/schemas.ts:12` — thread `PRRT_kwDOUA492s6chNGy`
  **[Coverage Gaps — G7, Low]** `dateOrTimestamp` accepts either a `YYYY-MM-DD` date or a full RFC3339 timestamp via `isValidRfc3339`. All passing tool-call tests use only date-only strings for `start_date`/`end_date`; no test passes a full timestamp as `start_date`/`end_date` and asserts it's accepted (not `isError`).

## Cluster: mcp/src/tools/list-time-entries.ts, mcp/src/tools/list-time-entries.test.ts

- **Q7** (Medium, Code Quality) `mcp/src/tools/list-time-entries.ts:36` — thread `PRRT_kwDOUA492s6chMf5`
  **[Code Quality — Q7, Medium]** `catch (err) { return toErrorResult(err as TogglApiError | TogglNetworkError); }` (also line 47) asserts the caught error's type without verification. errors.ts's buildErrorPayload does an `instanceof TogglApiError` check and otherwise unconditionally treats the error as a TogglNetworkError. If a future change (or an unforeseen bug) throws something else, it would be silently reported to the MCP client as `{ type: "network", operation: undefined }`, hiding the real defect from both the caller and any log output.
- **H3** (Medium, Regression & Hallucination) `mcp/src/tools/list-time-entries.ts:36` — thread `PRRT_kwDOUA492s6chMtB`
  **[Regression & Hallucination — H3, Medium]** Both catch blocks do `return toErrorResult(err as TogglApiError | TogglNetworkError)` — a compile-time cast with no runtime check. In current code the try blocks only wrap calls documented to throw exactly those two types, so this is low-risk today, but it is a forced cast masking a real type-safety gap: if a future change throws something else, it will be silently reported as a Toggl "network" error with `operation: undefined`, hiding the actual failure and losing the stack trace/error identity for debugging.
- **Q12** (Low, Code Quality) `mcp/src/tools/list-time-entries.ts:35` — thread `PRRT_kwDOUA492s6chMj_`
  **[Code Quality — Q12, Low]** Both catch blocks (lines 35-37 and 46-48) convert TogglApiError/TogglNetworkError into a tool result via toErrorResult but never call `log(...)`. An operator watching the server's stderr stream has zero visibility into failed Toggl calls unless the agent happens to surface the error text back to them — complicating debugging of a live session.
- **P3** (Low, Performance) `mcp/src/tools/list-time-entries.ts:39` — thread `PRRT_kwDOUA492s6chMns`
  **[Performance — P3, Low]** The handler does `rawEntries.filter(...)` to build `filtered`, then `filtered.some(...)` to check whether any entry has a project, then `filtered.map(...)` to curate — three full traversals of the same array where one pass would suffice. Impact is bounded by Toggl's 30 req/hour cap keeping realistic result sizes small, but it's a real redundant-traversal violation that gets worse if the unbounded date range (P2) is left unfixed.
- **P4** (Low, Performance) `mcp/src/tools/list-time-entries.ts:34` — thread `PRRT_kwDOUA492s6chMoB`
  **[Performance — P4, Low]** `TogglClient.listTimeEntries` calls `GET /me/time_entries`, which is not scoped to a single workspace — it returns entries across every workspace the account belongs to for the date range. `list-time-entries.ts:39` then discards everything not matching `effectiveWorkspaceId` client-side. For an account with multiple workspaces, the full cross-workspace payload is fetched, parsed, and held in memory before most of it is thrown away.
- **H1** (High, Regression & Hallucination) `mcp/src/tools/list-time-entries.ts:44` — thread `PRRT_kwDOUA492s6chMob`
  **[Regression & Hallucination — H1, High]** `getProjects()` returns `{ projects, warning? }`, and project-cache.ts fully implements the stale-cache-with-warning path. The only caller destructures `const { projects } = await getProjects(...)` and never reads or surfaces `warning`. spec.md (part of this same diff) lists TEM-12 as an active, non-dropped requirement marked "Verified," and its AC7 states the result must carry a `stale_cache` warning when the cache is stale and Toggl is unreachable but a stale cache exists; design.md's Error Handling Strategy table explicitly says the success result gains a `warnings` field. The shipped tool never does this.
- **R2** (High, Requirements) `mcp/src/tools/list-time-entries.ts:44` — thread `PRRT_kwDOUA492s6chMyb`
  **[Requirements — R2, High]** spec.md TEM-12 AC6 requires that when the cache is stale, refresh fails, and a stale cache is available, the system SHALL resolve project names against the stale cache and include a `stale_cache` warning (with the cache's age and the underlying failure) in the tool result. `getProjects` correctly computes and returns this `warning`, and it's unit-tested at that layer. However, `list-time-entries.ts:44` destructures only `{ projects }` from the `getProjects(...)` result and discards `warning` entirely — it is never attached to, or even referenced in, the JSON returned to the agent.
- **G2** (High, Coverage Gaps) `mcp/src/tools/list-time-entries.ts:43` — thread `PRRT_kwDOUA492s6chNBo`
  **[Coverage Gaps — G2, High]** When filtered entries contain a `project_id` and `getProjects` throws (e.g. no cache exists and the projects fetch itself fails with no stale fallback available), the handler catches the error and returns `toErrorResult(err)`. No test drives this path — the only Toggl-failure test fails on the first call (`listTimeEntries`), never on the project-lookup call.
- **P2** (Medium, Performance) `mcp/src/tools/schemas.ts:12` — thread `PRRT_kwDOUA492s6chMnZ`
  **[Performance — P2, Medium]** `dateOrTimestamp` only validates format and that `end_date >= start_date` — there is no maximum span enforced. A caller can request an arbitrarily wide range, and `TogglClient.listTimeEntries` has no pagination or limit either. Without a client-side cap, a wide-range call can pull an unbounded array into memory and serialize a very large JSON blob onto stdout in one message.
- **G1** (High, Coverage Gaps) `mcp/src/tools/schemas.ts:17` — thread `PRRT_kwDOUA492s6chNBL`
  **[Coverage Gaps — G1, High]** `positiveId` (`z.number().int().positive()`) rejects zero, negative, and non-integer `workspace_id` values, and `list-time-entries.ts` applies it to the optional `workspace_id` tool argument. No test ever passes an invalid `workspace_id` (e.g. `0`, `-1`, `1.5`) to `list_time_entries` and asserts `isError: true`. Only valid overrides are tested.
- **V2** (High, Coverage) `mcp/src/tools/list-time-entries.test.ts:180` — thread `PRRT_kwDOUA492s6chMzO`
  **[Coverage — V2, High]** `registerListTimeEntries` has two independent try/catch blocks — one around `client.listTimeEntries`, one around `getProjects`. Only the first is exercised ("Toggl 500 error is surfaced..."). No test makes `listTimeEntries` succeed with project_id-bearing entries and then makes the projects fetch fail, so the second catch branch has zero coverage. A regression there would not be caught by this suite.
- **V3** (Medium, Coverage) `mcp/src/tools/list-time-entries.test.ts:155` — thread `PRRT_kwDOUA492s6chMze`
  **[Coverage — V3, Medium]** The only project-resolution test ("entries with a project_id resolve project names via a pre-warmed cache...") pre-writes the cache before calling the tool, so it never exercises the tool wiring together with `getProjects`'s cold-cache/fetch/write path. The integration point — the tool triggering a live 2-request flow and correctly resolving a name from the freshly-fetched (not pre-seeded) cache — is untested.
- **V4** (Medium, Coverage) `mcp/src/tools/list-time-entries.test.ts:180` — thread `PRRT_kwDOUA492s6chM2p`
  **[Coverage — V4, Medium]** `errors.ts` maps both `TogglApiError` and `TogglNetworkError` to structured results, and `client.test.ts` unit-tests `TogglNetworkError` in isolation, but no `list-time-entries.test.ts` test drives a network failure through the full tool call to confirm the tool returns `{ error: { type: "network", ... } }` rather than crashing or leaking an unhandled rejection.
- **V14** (Low, Coverage) `mcp/src/tools/list-time-entries.test.ts:48` — thread `PRRT_kwDOUA492s6chNAg`
  **[Coverage — V14, Low]** The tool's `workspace_id` input uses `positiveId` (`z.number().int().positive()`), analogous to the `start_date`/`end_date` validation that IS tested. No test supplies an invalid `workspace_id` (e.g. `0`, `-1`, or a non-integer) to confirm it's rejected before any Toggl request, the same way the date-format and date-order tests do.
- **I2** (High, Isolation) `mcp/src/tools/list-time-entries.test.ts:32` — thread `PRRT_kwDOUA492s6chNKy`
  **[Isolation — I2, High]** In all 8 tests in this file, `startFakeToggl(...)` and `connectToolClient(...)` (which does real async work) execute before the `try` block. If `connectToolClient` throws (e.g. a tool-registration error), `fake.close()` in the `finally` is never reached, so the already-listening HTTP server is never closed. A leaked open handle can hang the whole test file at process-exit time, turning one setup failure into a suite-wide hang rather than a single clean test failure.
- **I3** (Medium, Isolation) `mcp/src/tools/list-time-entries.test.ts:30` — thread `PRRT_kwDOUA492s6chNLR`
  **[Isolation — I3, Medium]** `makeTmpCachePath()` (in test-harness.ts) calls `fsSync.mkdtempSync` against `os.tmpdir()` for every test. Only one test in this file removes its temp directory afterward; the other 7 tests leave their temp directories on disk permanently. Contrast with project-cache.test.ts, where every test's `makeTmpDir()` is removed in `finally`.

## Cluster: mcp/src/config.ts, mcp/src/config.test.ts, mcp/src/index.ts, mcp/src/index.test.ts, mcp/package.json

- **R1** (Critical, Requirements) `mcp/src/config.ts:1` — thread `PRRT_kwDOUA492s6chMyA`
  **[Requirements — R1, Critical]** spec.md TEM-01 AC1 requires the server to load `TOGGL_API_TOKEN`/`TOGGL_WORKSPACE_ID` "from the environment (including a `.env` file)". No `dotenv` dependency exists in `mcp/package.json`, no `dotenv.config()` call exists anywhere in `mcp/src`, and `loadConfig` reads only `process.env` as passed in from `index.ts`. Reproduced live: with a valid `mcp/.env` containing both required variables present in the `mcp/` working directory, running `node dist/index.js` still exits with code 1 and logs "TOGGL_API_TOKEN is required; TOGGL_WORKSPACE_ID is required" — the `.env` file is completely ignored. The README's Setup section instructs the user to `cp .env.sample .env` as if that alone configures the server, which is false.
- **H2** (High, Regression & Hallucination) `mcp/src/config.ts:21` — thread `PRRT_kwDOUA492s6chMsd`
  **[Regression & Hallucination — H2, High]** `loadConfig(env)` reads only from the `env` object passed in; no file-based `.env` loading happens anywhere in the codebase. spec.md TEM-01 AC1 (active, marked "Verified") requires the server to load `TOGGL_API_TOKEN`/`TOGGL_WORKSPACE_ID` "from the environment (including a `.env` file)", and design.md's Tech Decisions table names `dotenv` as the library for this. `mcp/README.md` step 3 instructs users to `cp .env.sample .env` and fill in credentials as if that file is read by the server, which is false — the `dotenv` npm dependency was listed in package.json from the very first scaffold commit but was never actually called, and was removed in the scope-reduction commit with a misleading message ("drop the now-unused dotenv dependency") implying it was newly unused rather than never used.
- **S3** (Low, Security) `mcp/src/config.ts:1` — thread `PRRT_kwDOUA492s6chMuj`
  **[Security — S3, Low]** `config.ts` reads credentials only from `process.env`; no `dotenv` dependency is declared in `mcp/package.json` and nothing in the source calls `dotenv.config()` or otherwise parses a `.env` file. The README and `.env.sample` instruct the operator to `cp .env.sample .env` and fill in `TOGGL_API_TOKEN`, implying the running process reads it directly, but a manually-run `node dist/index.js` will not pick it up at all. This mismatch between documented and actual credential-loading behavior is exactly the kind of gap that leads an operator to work around it insecurely (e.g. sourcing `.env` into the shell profile, or hardcoding the token into a wrapper script committed to version control).
- **G8** (Low, Coverage Gaps) `mcp/src/config.ts:48` — thread `PRRT_kwDOUA492s6chNHI`
  **[Coverage Gaps — G8, Low]** The cache-path resolution explicitly checks `env.TOGGL_CACHE_PATH.trim() !== ""`, implying a whitespace-only value should fall back to `DEFAULT_CACHE_PATH`. Existing tests only cover "unset" and "valid explicit path" — no test sets `TOGGL_CACHE_PATH` to `""` or `"   "` and asserts the default is used.
- **V9** (Low, Coverage) `mcp/src/config.test.ts:9` — thread `PRRT_kwDOUA492s6chM63`
  **[Coverage — V9, Low]** `config.ts` explicitly guards against whitespace-only values for TOGGL_API_TOKEN, TOGGL_WORKSPACE_ID, and TOGGL_CACHE_PATH, but no test supplies a whitespace-only string for any of them to prove the trim guards actually reject/fallback rather than silently accepting garbage.
- **M4** (Medium, Maintainability) `mcp/src/config.test.ts:9` — thread `PRRT_kwDOUA492s6chNU1`
  **[Maintainability — M4, Medium]** The tests "missing TOGGL_API_TOKEN", "missing TOGGL_WORKSPACE_ID", "non-numeric TOGGL_WORKSPACE_ID", "zero TOGGL_WORKSPACE_ID", and "negative TOGGL_WORKSPACE_ID" all follow the identical shape — build an `env` object, assert.throws with a predicate checking `instanceof ConfigError` and a regex against the message — differing only in the input `env` and the expected regex. This is the textbook copy-pasted-test-bodies anti-pattern; adding a 6th invalid-workspace-id case means copying another 7-line block.
- **G9** (Low, Coverage Gaps) `mcp/src/index.ts:31` — thread `PRRT_kwDOUA492s6chNHe`
  **[Coverage Gaps — G9, Low]** The final `main().catch(...)` handler (which logs a "fatal error" line and exits with code 1 for any error not handled inside `main`, e.g. `server.connect` throwing) has no test forcing this path — index.test.ts only covers the ConfigError exit path and the happy-path handshake.
- **I4** (Medium, Isolation) `mcp/src/index.test.ts:14` — thread `PRRT_kwDOUA492s6chNMA`
  **[Isolation — I4, Medium]** Same leak pattern as the list-time-entries.test.ts case — `tmpCachePath()` creates a real directory via `mkdtempSync` and none of the 3 tests in this file remove it. Combined with the other leak, every full test run leaves 10 abandoned directories under the OS temp folder.
- **I5** (Low, Isolation) `mcp/src/index.test.ts:66` — thread `PRRT_kwDOUA492s6chNMn`
  **[Isolation — I5, Low]** The first two tests explicitly await the child's 'exit' event, proving process cleanup. The third test only calls `await client.close()` on the `StdioClientTransport` and never verifies the underlying spawned `index.js` process actually exits. If the server doesn't tear down cleanly on stdin EOF, this test would still pass while silently leaking an orphaned Node process per run.
- **M7** (Low, Maintainability) `mcp/src/index.test.ts:14` — thread `PRRT_kwDOUA492s6chNVy`
  **[Maintainability — M7, Low]** `tmpCachePath()` does exactly what `makeTmpCachePath()` in `mcp/src/tools/test-harness.ts` already does (mkdtemp + join with "projects.json"), differing only in the tmp-dir prefix string. Minor, but it's a fourth occurrence of essentially the same tmp-cache-path helper pattern across the suite.
- **TP1** (Medium, Performance (Tests)) `mcp/src/index.test.ts:1` — thread `PRRT_kwDOUA492s6chNPf`
  **[Performance (Tests) — TP1, Medium]** This file's 3 tests each spawn a real OS process, incurring Node startup + module-load + stdio handshake latency, and run in the same flat test glob as the pure-unit tests and the in-memory tool tests. There's no npm script or naming convention to run just the fast subset, so `npm test` always pays the cumulative process-spawn cost. The third test ("valid env: successful MCP handshake") specifically doesn't need a real subprocess — it isn't testing process exit code or stderr, only tool discovery — and could reuse `connectToolClient`'s `InMemoryTransport` harness already used elsewhere, which is materially cheaper.

## Cluster: mcp/src/cache/project-cache.ts, mcp/src/cache/project-cache.test.ts

- **Q1** (High, Code Quality) `mcp/src/cache/project-cache.ts:56` — thread `PRRT_kwDOUA492s6chMbu`
  **[Code Quality — Q1, High]** writeCache (and the whole module) never calls fs.mkdir on the cache path's parent directory. The default cache path is `~/.cache/toggl-mcp/projects.json` (config.ts:17) — a directory that does not exist by default on a fresh machine. On first use, `fs.writeFile(tmpPath, ...)` throws ENOENT, which is swallowed by writeCache's catch and only logged — the tool call still "succeeds" per the designed degrade path, but the cache NEVER persists. Every subsequent list_time_entries call with a project-bearing entry re-fetches the full project list from Toggl, permanently defeating the TTL cache.
- **Q3** (Medium, Code Quality) `mcp/src/cache/project-cache.ts:73` — thread `PRRT_kwDOUA492s6chMej`
  **[Code Quality — Q3, Medium]** readCache only checks `typeof parsed.fetchedAt !== "string"`, not that it's a valid date. If fetchedAt is a string Date.parse cannot parse (e.g. cache corruption), `Date.parse(cached.fetchedAt)` is NaN, so `Date.now() - NaN >= SEVEN_DAYS_MS` evaluates to false — `stale` becomes false even though the timestamp is garbage, and the cache is treated as fresh forever, never refetching.
- **G4** (Medium, Coverage Gaps) `mcp/src/cache/project-cache.ts:73` — thread `PRRT_kwDOUA492s6chNC-`
  **[Coverage Gaps — G4, Medium]** `stale` is computed as `Date.now() - Date.parse(cached.fetchedAt) >= SEVEN_DAYS_MS`. If `fetchedAt` fails `Date.parse` (returns NaN), the subtraction yields NaN, and `NaN >= SEVEN_DAYS_MS` evaluates to false — meaning the cache would be treated as permanently fresh and never refreshed. `readCache` only validates `typeof parsed.fetchedAt === "string"`, not that it's a valid date, so this is reachable via a corrupted cache file. No test covers a cache file with a non-date `fetchedAt` string.
- **Q4** (Medium, Code Quality) `mcp/src/cache/project-cache.ts:32` — thread `PRRT_kwDOUA492s6chMe3`
  **[Code Quality — Q4, Medium]** toCachedProject defaults a missing `id`/`workspace_id` to `0`, `name` to `""`, and `active` to `false` rather than rejecting/flagging a malformed project record. A project silently coerced to `id: 0` could collide with an unrelated time entry whose `project_id` happens to be `0`/falsy, producing an incorrect resolved project name without any warning — masking bad upstream data instead of surfacing it.
- **G5** (Medium, Coverage Gaps) `mcp/src/cache/project-cache.ts:30` — thread `PRRT_kwDOUA492s6chNF3`
  **[Coverage Gaps — G5, Medium]** All raw-project fixtures in project-cache.test.ts supply complete `id`/`name`/`active`/`workspace_id` fields. No test exercises a raw Toggl project response missing one of these fields to confirm the explicit `?? 0` / `?? ""` / `?? false` defaults in `toCachedProject` are applied as intended.
- **Q5** (Low, Code Quality) `mcp/src/cache/project-cache.ts:59` — thread `PRRT_kwDOUA492s6chMfF`
  **[Code Quality — Q5, Low]** writeCache's try/catch wraps both writeFile and rename. If writeFile succeeds but rename then fails (e.g. cross-device rename, permissions changed mid-write), the temp file is left on disk permanently with no cleanup — a slow, silent disk-litter bug in the one persistence path this server has.
- **S1** (Low, Security) `mcp/src/cache/project-cache.ts:59` — thread `PRRT_kwDOUA492s6chMth`
  **[Security — S1, Low]** `writeCache` calls `fs.writeFile(tmpPath, JSON.stringify(file), "utf8")` with no explicit `mode`, so both the temp file and the file it's renamed into inherit the process's default umask-based permissions (typically 0644 — group/world readable). The cache stores Toggl project names, which can embed client/business names. On a shared/multi-user machine, this file is readable by other local users even though it holds business-sensitive naming data.
- **Q10** (Low, Code Quality) `mcp/src/cache/project-cache.ts:66` — thread `PRRT_kwDOUA492s6chMjD`
  **[Code Quality — Q10, Low]** None of the package's exported functions — `getProjects`, `loadConfig`, `toErrorResult`, `toCuratedEntry`, the TogglClient methods — carry a doc comment describing parameters, return shape, or thrown-error contract. For a package that is this repo's first TypeScript precedent, the lack of any doc-comment convention leaves future TS packages nothing to follow.
- **V7** (Medium, Coverage) `mcp/src/cache/project-cache.test.ts:149` — thread `PRRT_kwDOUA492s6chM3g`
  **[Coverage — V7, Medium]** `readCache` only checks `Array.isArray(parsed.projects)`, not that each element has `id`/`name`/`active`/`workspaceId`. The existing test only covers the top-level-missing-array case, not a top-level-valid-but-malformed-items case. Such a file would be treated as a cache hit and returned to callers with a project object missing required fields.
- **V13** (Low, Coverage) `mcp/src/cache/project-cache.test.ts:94` — thread `PRRT_kwDOUA492s6chM8X`
  **[Coverage — V13, Low]** The staleness check is `Date.now() - Date.parse(cached.fetchedAt) >= SEVEN_DAYS_MS`. Tests only cover "now" (clearly fresh) and "8 days ago" (clearly stale) — the `>=` boundary at exactly 7 days is never probed, so an off-by-one (e.g. `>` instead of `>=`) would not be caught.
- **I1** (High, Isolation) `mcp/src/cache/project-cache.test.ts:171` — thread `PRRT_kwDOUA492s6chNKW`
  **[Isolation — I1, High]** The "unwritable cache directory" test calls `fsSync.chmodSync(dir, 0o555)` to force `writeCache`'s `fs.writeFile` to fail, then asserts `console.error` was called and a stale-but-fresh result is returned. On any environment running as root (the default for most official Node Docker images used in CI), chmod permission bits are not enforced against root's own writes, so the write silently succeeds, no error is logged, and the assertion fails non-deterministically depending on the runner, not the code under test.
- **M1** (High, Maintainability) `mcp/src/cache/project-cache.test.ts:171` — thread `PRRT_kwDOUA492s6chNQo`
  **[Maintainability — M1, High]** The "unwritable cache directory" test does `fsSync.chmodSync(dir, 0o555)` to force a write failure and expects `getProjects` to log via `console.error` and still return the freshly-fetched list. On any environment running as root — the default for most official Node Docker images used in CI — chmod permission bits are not enforced against root's own writes, so `writeCache` will actually succeed, `console.error` will never be called, and the assertion will fail non-deterministically depending on the runner, not the code under test. No CI pipeline exists yet in this repo, so this risk is currently latent but will surface the moment one is added.
- **C2** (Low, Clarity) `mcp/src/cache/project-cache.test.ts:201` — thread `PRRT_kwDOUA492s6chNQa`
  **[Clarity — C2, Low]** An earlier assertion already establishes `result.warning` is truthy. A later assertion re-checks the same nullability inside a compound `&&` condition. Should the numeric comparison ever fail, the message is just "assert.ok(false)" with no indication of the actual value, since `assert.ok` doesn't print operand values.
- **M2** (Medium, Maintainability) `mcp/src/cache/project-cache.test.ts:12` — thread `PRRT_kwDOUA492s6chNQ8`
  **[Maintainability — M2, Medium]** `FakeServer`, `startProjectsServer`, and `sendJson` duplicate `mcp/src/tools/test-harness.ts`'s `FakeTogglServer`/`startFakeToggl`/`sendJson` almost verbatim, just tracking a `requestCount` instead of the full `requests` array. Any future fix to the fake server now has to be applied in two places and can silently drift.

## Cluster: mcp/src/toggl/client.ts, mcp/src/toggl/client.test.ts

- **A1** (Medium, Architecture) `mcp/src/toggl/client.ts:63` — thread `PRRT_kwDOUA492s6chMZx`
  **[Architecture — A1, Medium]** The private `request<T>(method, path, operation, body?)` method still carries generic `method`/`body` parameters, JSON-serialization, and conditional `Content-Type` header logic (lines 67, 75, 77) sized for the original create/update/delete surface. After the scope cut, the only two call sites (listTimeEntries, listProjects) are both hardcoded "GET" with no body — this is speculative generality left over from the dropped write paths, violating the project's own Simplicity First / YAGNI principle for a read-only client.
- **A2** (Medium, Architecture) `mcp/src/toggl/client.ts:71` — thread `PRRT_kwDOUA492s6chMaT`
  **[Architecture — A2, Medium]** TogglClient.request awaits `fetch(...)` with no AbortController/timeout. A hung TCP connection or an unresponsive Toggl endpoint would block the tool call — and the whole single-threaded stdio server — indefinitely, with no way for the agent/client to know the call is stuck versus slow. The design's Out-of-Scope table explicitly excludes retry/backoff/throttling, but a bounded timeout is a distinct resiliency concern not addressed anywhere in the design docs or code.
- **P1** (High, Performance) `mcp/src/toggl/client.ts:71` — thread `PRRT_kwDOUA492s6chMnE`
  **[Performance — P1, High]** `TogglClient.request()` calls `fetch()` with no `AbortSignal`/timeout of any kind. Since Toggl's own rate limiting or an outage can cause the API to hang or respond very slowly, a single stalled request will leave the tool call pending indefinitely. This blocks the entire tool call — and this is a single-process stdio server, so nothing else can proceed until it resolves or the process is killed.
- **S2** (Low, Security) `mcp/src/toggl/client.ts:71` — thread `PRRT_kwDOUA492s6chMuL`
  **[Security — S2, Low]** The `fetch()` call in `TogglClient.request` has no `AbortSignal`/timeout. If the Toggl endpoint (or a network intermediary) stalls instead of responding, the tool call hangs indefinitely with no recovery path other than killing the MCP server process — a resource-exhaustion/availability gap, however low-impact given this is a single-user local stdio server.
- **Q8** (Low, Code Quality) `mcp/src/toggl/client.ts:20` — thread `PRRT_kwDOUA492s6chMiR`
  **[Code Quality — Q8, Low]** `declare readonly retryAfter?: string;` is a deliberate, non-obvious TypeScript pattern (it suppresses the class-field auto-initialization to `undefined` so the property is genuinely absent — not present-but-undefined — when Toggl sends no Retry-After header). Nothing in the code explains why `declare` is used here instead of a plain field declaration; a future maintainer is likely to "simplify" it away and reintroduce the always-present-key behavior.
- **Q9** (Low, Code Quality) `mcp/src/toggl/client.ts:119` — thread `PRRT_kwDOUA492s6chMiv`
  **[Code Quality — Q9, Low]** listTimeEntries builds its query string via `URLSearchParams`, while listProjects hardcodes `?include_archived=false` directly in the path literal. Both are simple GET calls in the same class; the two different construction styles are an avoidable inconsistency within a single small file.
- **G3** (Medium, Coverage Gaps) `mcp/src/toggl/client.ts:85` — thread `PRRT_kwDOUA492s6chNCI`
  **[Coverage Gaps — G3, Medium]** The client attempts `JSON.parse(rawBody)` and falls back to the raw string on parse failure, or to `undefined` when the body is empty. No test in client.test.ts sends a non-JSON body (e.g. an HTML error page or plain text) or an empty body to verify `parsedBody` degrades to the raw string / `undefined` rather than throwing.
- **V6** (Medium, Coverage) `mcp/src/toggl/client.test.ts:51` — thread `PRRT_kwDOUA492s6chM3Q`
  **[Coverage — V6, Medium]** `client.ts`'s `request()` has two body-parsing branches never hit by any test: (a) falling back to the raw string when the response text isn't valid JSON, and (b) `parsedBody = undefined` when the body is empty. Every fake-server handler in this file uses `sendJson`, so both branches are dead code as far as the test suite is concerned.
- **V12** (Low, Coverage) `mcp/src/toggl/client.test.ts:53` — thread `PRRT_kwDOUA492s6chM72`
  **[Coverage — V12, Low]** `request()`'s `body` parameter and the conditional `Content-Type: application/json` header / `JSON.stringify(body)` call are never exercised — neither `listTimeEntries` nor `listProjects` (the only two public methods) ever pass a body, and no test calls `request` directly.
- **M3** (Medium, Maintainability) `mcp/src/toggl/client.test.ts:7` — thread `PRRT_kwDOUA492s6chNUX`
  **[Maintainability — M3, Medium]** `RecordedRequest`, `FakeServer`, `startFakeServer`, and `sendJson` are a near-line-for-line copy of `mcp/src/tools/test-harness.ts`'s equivalents (including the identical chunk-accumulation body-parsing logic). This is now the third independent copy of the same fake-server plumbing across the test suite (test-harness.ts, project-cache.test.ts, this file).

## Cluster: mcp/src/time-entries/curate.ts, mcp/src/time-entries/curate.test.ts

- **G6** (Low, Coverage Gaps) `mcp/src/time-entries/curate.ts:22` — thread `PRRT_kwDOUA492s6chNGV`
  **[Coverage Gaps — G6, Low]** `entry.id ?? 0`, `entry.description ?? ""`, and `entry.start ?? ""` are explicit defensive defaults, but every test entry in curate.test.ts supplies all three fields. No test verifies the fallback values when they're missing/null on the raw entry.
- **V1** (High, Coverage) `mcp/src/time-entries/curate.test.ts:6` — thread `PRRT_kwDOUA492s6chMy-`
  **[Coverage — V1, High]** `CuratedTimeEntry.stop` is typed `string | null` specifically for Toggl's running-entry case (`entry.stop ?? null`), but every test fixture in this file sets a concrete `stop` value. No test ever passes `stop: null` or an entry with `stop` absent. The same gap exists in `list-time-entries.test.ts`. A regression that breaks handling of running entries (Toggl's most common live-data case) would pass the whole suite undetected.
- **V8** (Medium, Coverage) `mcp/src/time-entries/curate.test.ts:26` — thread `PRRT_kwDOUA492s6chM32`
  **[Coverage — V8, Medium]** `toCuratedEntry`'s logic is `projectId == null ? null : (entry.project_name ?? ...)` — when `project_id` is null/absent, `project_name` is never consulted even if present on the entry, due to the short-circuit. The existing tests both use entries without `project_name` set, so this combination is never exercised. It's unclear whether this is intended behavior or a bug.
- **V10** (Low, Coverage) `mcp/src/time-entries/curate.test.ts:33` — thread `PRRT_kwDOUA492s6chM7W`
  **[Coverage — V10, Low]** `curate.ts` defends against missing `id` (`?? 0`), `description` (`?? ""`), and `start` (`?? ""`), but no test constructs an entry with any of these fields absent — only `project_id` absence is tested via destructuring.
- **C1** (Medium, Clarity) `mcp/src/time-entries/curate.test.ts:48` — thread `PRRT_kwDOUA492s6chNQH`
  **[Clarity — C1, Medium]** The test name reads "entry carrying its own project_name resolves the project even when the cache is empty (explicit project_id bypasses the cache, TEM-16 AC7)". The behavior actually under test is that `entry.project_name` — not `project_id` — takes precedence over the `projectsById` cache lookup. `project_id` is present in both this test and the cache-empty case; it plays no special "bypass" role here. A reader relying on the test name as documentation will misunderstand which field drives the behavior.

## Cluster: mcp/src/errors.test.ts

- **V11** (Low, Coverage) `mcp/src/errors.test.ts:13` — thread `PRRT_kwDOUA492s6chM7r`
  **[Coverage — V11, Low]** `errors.ts`'s 404 branch never includes `retryAfter` in its output regardless of whether the error carries one — but no test constructs a 404 `TogglApiError` with `retryAfter` set to confirm this drop is intentional (vs. an accidental omission in the 404 branch's object literal).
- **M5** (Low, Maintainability) `mcp/src/errors.test.ts:6` — thread `PRRT_kwDOUA492s6chNVQ`
  **[Maintainability — M5, Low]** The shared `parse()` helper performs `assert.equal(result.content.length, 1)` and `assert.equal(item.type, "text")` internally before returning the parsed JSON body, rather than exposing that shape check to each call site. If `toErrorResult`'s content-array shape ever regresses, every one of the five tests using `parse()` fails inside the shared helper instead of reporting at the call site.
- **M6** (Low, Maintainability) `mcp/src/errors.test.ts:94` — thread `PRRT_kwDOUA492s6chNVi`
  **[Maintainability — M6, Low]** This test re-asserts `isError === true` for a `TogglApiError` and a `TogglNetworkError`, both of which are already individually verified as `isError === true` in the four preceding tests. It adds maintenance surface without covering any new branch or behavior.

## Cluster: mcp/src/logger.test.ts

- **V5** (Medium, Coverage) `mcp/src/logger.test.ts:32` — thread `PRRT_kwDOUA492s6chM29`
  **[Coverage — V5, Medium]** The only test checking serialized output shape always passes a `meta` argument. None of the calls without `meta` inspect the emitted JSON string — they only check `callCount`. `logger.ts` explicitly guards `if (meta !== undefined) { line.meta = meta; }` to keep the key genuinely absent when omitted, but that guard is never verified.

## Cluster: mcp/.env.sample

- **Q2** (High, Code Quality) `mcp/.env.sample:6` — thread `PRRT_kwDOUA492s6chMcN`
  **[Code Quality — Q2, High]** The comment directly above (`# Optional - documented defaults apply when unset`) implies this line should be commented out, but it is left as a live `KEY=value` assignment. README.md's Setup step 3 tells users to `cp .env.sample .env` — so by default, `TOGGL_CACHE_PATH` becomes the literal string `~/.cache/toggl-mcp/projects.json` with the tilde unexpanded. config.ts:48-50 takes any non-empty env value verbatim (no tilde expansion), so the cache path is treated as a literal directory named `~` relative to the server's CWD — never the home directory. This silently breaks cache persistence for anyone who follows the documented setup exactly.

## Cluster: .specs/features/TOGGL-2-time-entries-mcp/design.md

- **Q11** (Low, Code Quality) `.specs/features/TOGGL-2-time-entries-mcp/design.md:19` — thread `PRRT_kwDOUA492s6chMjq`
  **[Code Quality — Q11, Low]** The top-of-file Revision note tells readers to mentally filter out create/update/delete/matching content, but the Mermaid diagram itself still depicts `T1[list/get/create/update/delete_time_entry]`, `T2[refresh_projects]`, and the matching/cache flow exactly as originally designed — none of it reflects the current single-tool, read-only shape. A reader who looks only at the diagram gets an actively wrong picture of the current architecture.
